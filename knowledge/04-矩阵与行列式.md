# 矩阵与行列式

二阶行列式：|a b; c d| = ad - bc。

三阶行列式可以用对角线法则（萨吕斯法则）展开。

行列式性质：

- 交换两行（列），行列式变号；
- 某行（列）乘以 k，行列式乘以 k；
- 两行（列）成比例，行列式为 0。

矩阵可逆的充要条件是其行列式不为 0，逆矩阵 A^(-1) = adj(A) / det(A)。

克拉默法则：对线性方程组 Ax = b，若 det(A) ≠ 0，则
x_i = det(A_i) / det(A)，其中 A_i 是把 A 的第 i 列换成 b 得到的矩阵。
