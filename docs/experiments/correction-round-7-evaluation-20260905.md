# Round 7 微调、合并与三组评测报告（2026-09-05）

## 1. 实验概况

本轮以 Round 6 合并模型为唯一基座，继续进行一轮纠错微调。训练完成后选择
验证损失最低的 checkpoint，合并 LoRA，并使用同一个 Round 7 合并模型评测原始
测试集、Round 7 纠错验证集和历史回归集。本轮没有进行双模型对比。

主要结果目录：

```text
eval/results/correction-round-7-evaluation-20260905/
```

完整评测结果保留在本地和云端，模型权重不提交到 GitHub。

## 2. 训练数据与配置

| 项目 | 设置 |
|---|---|
| 基座模型 | `outputs/correction-round-6-merged` |
| 微调方式 | QLoRA 4-bit + LoRA |
| 训练数据 | 215 条 |
| 新纠错数据 | 150 条，约 69.77% |
| 原始数据回放 | 65 条，约 30.23%，按来源分层抽样 |
| 内部验证集 | 40 条普通原始数据 |
| 纠错验证集 | 30 条，训练时不使用 |
| epoch | 1 |
| batch size | 1 |
| 梯度累积 | 8 |
| learning rate | `5e-6` |
| 最大训练序列长度 | 2048 |

Round 4、Round 5 和 Round 6 的旧纠错训练源没有进入本轮 staging。原始最终测试集
和历史回归集也没有进入训练或内部验证。纠错题只禁止完全相同题目，允许同一能力点
的相似变体保留，以形成有效的错误模式覆盖。

本轮配置和数据整理入口为：

```text
configs/training/correction-round-7-20260905/train_config.yaml
scripts/build_correction_round7.py
scripts/build_round7_staging.py
data/processed/round-7-staging/manifest.json
```

## 3. 训练结果

| 指标 | 结果 |
|---|---:|
| 优化步数 | 27 |
| 最终 train loss | 0.65394 |
| 最终 eval loss | 0.25585 |
| 最低 eval loss | 0.25510 |
| 最优 checkpoint | `outputs/correction-round-7/checkpoint-10` |
| 训练状态 | 正常完成 |
| 训练耗时 | 87.31 秒 |

训练没有出现 CUDA OOM、NaN、进程中断或数据加载失败。训练完成后，最优 checkpoint
已合并到：

```text
outputs/correction-round-7-merged
```

合并模型经权重分片校验，可以被 vLLM 加载。

需要注意：当前 Transformers 版本不支持配置中的 `warmup_ratio`，日志提示实际回退为
0 个 warmup step。因此本轮结果可以使用，但下一轮如果继续训练，应改成显式的
`warmup_steps`，避免配置与实际执行不一致。

## 4. 程序化评测结果

三组评测均由 `eval/evaluate.py` 完成，vLLM 服务模型为
`outputs/correction-round-7-merged`，上下文上限为 4096，评测 `max_tokens=2048`，
温度为 0。三组请求全部成功，`failed=0`。

| 数据集 | 总数 | 正确 | 错误 | 准确率 | 平均延迟 | 首 token 延迟 |
|---|---:|---:|---:|---:|---:|---:|
| 原始测试集 | 116 | 77 | 39 | 66.38% | 3579.35 ms | 66.23 ms |
| Round 7 纠错验证集 | 30 | 24 | 6 | 80.00% | 2221.19 ms | 60.56 ms |
| 历史回归集 | 42 | 16 | 26 | 38.10% | 5030.52 ms | 63.29 ms |

逐题结果位置：

```text
eval/results/correction-round-7-evaluation-20260905/original-test/
eval/results/correction-round-7-evaluation-20260905/correction-validation/
eval/results/correction-round-7-evaluation-20260905/regression/
```

每组目录包含 `report.json`、`details.json`、`bad_cases.json`、两张评测图和
`run_config.json`。

## 5. 与 Round 6 的变化

依据 Round 6 报告中的程序化指标进行参考比较：

| 数据集 | Round 6 | Round 7 | 变化 |
|---|---:|---:|---:|
| 原始测试集 | 65.52% | 66.38% | +0.86 个百分点 |
| 纠错验证集 | 70.00% | 80.00% | +10.00 个百分点* |
| 历史回归集 | 40.48% | 38.10% | -2.38 个百分点 |

\* 两轮纠错验证集题目内容不同，不能把这项变化视为严格的同题提升。

原始测试集只有小幅变化，回归集略有下降；Round 7 的新增纠错验证集表现较好，说明
新加入的错误模式训练产生了一定效果，但尚未充分迁移到原始测试集和历史错误题。

## 6. 现状分析

1. **训练链路正常。** 模型加载、QLoRA 训练、checkpoint 保存、LoRA 合并、vLLM
   部署和三组推理请求均成功。
2. **纠错方向有改善迹象。** Round7 独立纠错验证集达到 80%，但样本只有 30 条，
   只能说明本轮覆盖的能力点有效，不能代表整体数学能力。
3. **通用能力没有明显提升。** 原始测试集 66.38%，仅比 Round6 的程序化结果高
   0.86 个百分点，属于小幅波动，不能称为显著提升。
4. **回归集仍是主要短板。** 回归集 42 条中有 26 条错误，表明历史错误的修复还不
   稳定；不能只看纠错验证集判断模型已经完成纠错。
5. **指标需要语义复核。** 程序化准确率会受最终答案格式、单位、LaTeX 表达和答案
   提取影响。本报告未包含 LLM Judge 结果，后续应对三组的 `details.json` 做语义评测，
   并人工抽查不确定项和关键错题。

## 7. 下一步建议

1. 先对三组结果运行 LLM Judge，分别保存到各自目录的 `llm-judge/`，再人工检查
   错误率最高的题目，区分数学错误、答案截断和格式误判。
2. 不建议马上提高学习率或增加 epoch。Round 7 已经是从合并模型继续训练，且回归集
   下降；下一轮应优先改善数据质量和覆盖，而不是加大更新幅度。
3. 继续采用“新纠错题为主、少量原始回放”的策略，但保留约 25%～35% 原始回放，
   防止通用能力遗忘；不要再次完整重复全部基础训练集。
4. 回归集原题继续只用于检验，不直接加入训练。围绕回归错误生成不同数字、不同
   表述、不同难度的同类型训练题，并单独保留新的纠错验证集。
5. 下一轮配置应使用显式 `warmup_steps`，并继续只保留一个模型，避免云端存储和
   显存不足。模型选择仍以内部 `eval_loss`、原始测试集和回归集的综合结果为依据。

## 8. 结论

Round 7 的训练和部署流程完整、稳定，纠错验证集有较好表现，但原始测试集没有显著
提升，历史回归集反而略降。因此当前最合理的结论是：**本轮训练成功，但模型的纠错
能力尚未稳定迁移到旧错误和通用测试题。** 下一步应先完成 LLM Judge 与人工复核，
再根据真实数学错误补充高质量同类型数据，而不是直接增加训练轮数或学习率。
