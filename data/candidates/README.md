# 候选数据归档

本目录保存已经收集、但尚未完成全部人工验算的数据候选文件。这里的文件只用于审计和后续筛选，`scripts/prepare_data.py` 不会自动读取本目录。

- `correction-round-3-all.jsonl`：第三轮 100 条官方来源候选题的完整归档。
- 真正进入训练的文件必须先经过人工抽查和独立验算，再放入 `data/raw/corrections/`。

这样可以保留原始候选记录，同时避免未审核数据或纠错验证题意外参与训练。
